import { colors } from "./color";
import { space, lineHeights } from "./spacing";
import { sizes } from "./sizes";
import { fonts, fontWeights, fontSizes } from "./font";

export const theme = {
    colors,
    space,
    lineHeights,
    sizes,
    fonts,
    fontSizes,
    fontWeights,
};